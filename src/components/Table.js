import React, { useState } from "react";

const Table = (props) => {
  const [tableData, setTableData] = useState();

  return (
    <div>
      <h2>Table</h2>
    </div>
  );
};

export default Table;
